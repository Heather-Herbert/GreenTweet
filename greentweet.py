import socialmedia
import ai
import web

import os
from dotenv import load_dotenv

import logging
import datetime


def main():
    # Set application up
    logname = datetime.today().strftime('%Y-%m-%d')
    logging.basicConfig(filename=f'log/main-{logname}.log', level=logging.INFO)


    # Get URL

    # Get Text and Image of article

    # Build reply

    # Send out response

    # Tidy up

if __name__ == "__main__":
    main()